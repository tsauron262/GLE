extends:
    module: bimpcommercial
    object_name: BimpComm

objects:
    bimp_comm:
        instance:
            bimp_object: Bimp_Commande
            id_object:
                request: id
    
    commande:
        instance:
            bimp_object: Bimp_Commande
            id_object:
                request: id

sections:
    maintabs:
        tabs:
            default:
                sections:
                    mainnav:
                        nav_tabs:
                            demandes_fin:
                                show:
                                    field_value:
                                        object: commande
                                        field_name: id_demande_fin
                                ajax_render_method:
                                    object: commande
                                    method: renderDemandeFinancementView
            logistic:
                label: Logistique
                show:
                    callback:
                        method: isLogistiqueActive
                        object: commande
                module: bimplogistique
                controller: commande
                url_params:
                    id:
                        request: id
            #            prepacommande:
            #                label: Prepa/Commande
            #                url: ../Synopsis_PrepaCommande/prepacommande.php?new=1
            #                url_params:
            #                    id:
            #                        request: id
            support:
                label: Support
                sections:
                    -   type: custom
                        custom:
                            callback:
                                method: renderSupport
                show:
                    callback:
                        method: asSupport

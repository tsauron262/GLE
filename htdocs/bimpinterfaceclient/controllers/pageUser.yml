bootstrap: 1

objects:
    userClient:
        instance:
            bimp_object: BIC_UserClient
            id_object: 
                request: id
    user_contrat:
        instance:
            bimp_obejct: BIC_UserClientContrats
    
    tickets:
        instance:
            bimp_object: BIC_UserTickets

sections:
    - type: object_header
      object_header: 
          object: userClient
    - type: view
      view:
          name: for_controller
          object: userClient
